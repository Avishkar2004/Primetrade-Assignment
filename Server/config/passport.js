/**
 * Passport.js authentication config (optional).
 * This app uses JWT via middleware/auth.js. Add passport + passport-jwt
 * and configure strategies here if you switch to Passport later.
 */
export default {};
